[project]
name = "sqlmesh-fabric"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "sqlmesh",
]

[tool.uv.sources]
sqlmesh = { git = "https://github.com/mattiasthalen/sqlmesh.git", rev = "add-support-for-fabric" }
